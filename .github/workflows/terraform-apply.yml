# =============================================================================
# CertiVault ΓÇö Terraform Apply (on push to main)
# =============================================================================
# Applies the Terraform plan ONLY after code lands on main AND the push
# touched files under terraform/.
# Last triggered: 2026-07-19 ΓÇö force fresh run after re-run loop on stale SHA
#
# ΓöÇΓöÇ Required GitHub Repository Secrets ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
# Configure these at: Settings ΓåÆ Secrets and variables ΓåÆ Actions ΓåÆ New secret
#
#   AWS_ACCESS_KEY_ID         ΓåÆ IAM user access key with Terraform permissions
#   AWS_SECRET_ACCESS_KEY     ΓåÆ IAM user secret access key
#   AWS_REGION                ΓåÆ AWS region (e.g. ap-south-1)
#                               NOTE: currently hardcoded to ap-south-1 in the
#                               workflow env block; set this secret if you want
#                               to make it configurable.
#
# ΓöÇΓöÇ S3 Backend Secrets (if overriding backend.tf values via CI) ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
# These are NOT required if backend.tf already contains the hard-coded bucket
# and table names (which it does for this project). They are documented here
# for teams that prefer to pass backend config dynamically:
#
#   TF_STATE_BUCKET           ΓåÆ S3 bucket name for remote state
#                               (default: certivault-terraform-state)
#   TF_LOCK_TABLE             ΓåÆ DynamoDB table name for state locking
#                               (default: certivault-terraform-locks)
#
# ΓöÇΓöÇ IAM Permissions Required ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
# The IAM user/role must have at minimum:
#   - AmazonEC2FullAccess (VPC, subnets, security groups)
#   - AmazonEKSFullAccess
#   - AmazonElastiCacheFullAccess
#   - AmazonS3FullAccess (state bucket + document bucket)
#   - AmazonDynamoDBFullAccess (state lock table)
#   - AmazonECRFullAccess
#   - IAMFullAccess (for EKS node role, OIDC provider)
#   - SecretsManagerFullAccess (for DocumentDB password)
#   - AmazonDocDBFullAccess
#
# ΓöÇΓöÇ Pre-requisites (one-time bootstrap) ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
# The S3 backend bucket and DynamoDB lock table must exist before the first
# terraform init. See backend.tf for the aws CLI commands to create them.
#
# ΓöÇΓöÇ Safety features ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
#   - Skips gracefully (exit 0) when AWS credentials are not configured
#   - Runs a fresh plan first ΓÇö never applies without it
#   - Uses -auto-approve only inside CI (TF_IN_AUTOMATION=true)
#   - On failure: outputs terraform state list to help diagnose drift
#   - Manual workflow_dispatch requires typing "APPLY" to confirm
# =============================================================================

name: Terraform Apply

on:
  push:
    branches: [main]
    paths:
      - "terraform/**"
      - ".github/workflows/terraform-apply.yml"
  workflow_dispatch:
    inputs:
      confirm:
        description: "Type APPLY to confirm you want to apply Terraform changes"
        required: true

concurrency:
  # Never run two applies at the same time
  group: terraform-apply
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write   # for OIDC auth (future ΓÇö when moving off long-lived keys)

jobs:
  terraform-apply:
    name: Terraform Apply
    runs-on: ubuntu-latest

    # Block manual dispatch unless user typed APPLY
    if: >
      github.event_name == 'push' ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.confirm == 'APPLY')

    defaults:
      run:
        working-directory: terraform

    env:
      TF_IN_AUTOMATION: "true"
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_DEFAULT_REGION: ap-south-1

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ΓöÇΓöÇ Guard: skip entire job if AWS credentials are not configured ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
      # Without this, terraform init produces a confusing "no valid credential
      # sources" error. Instead we exit 0 with a clear message so the job shows
      # as skipped rather than failed.
      - name: Check AWS credentials are configured
        id: check-creds
        shell: bash
        run: |
          if [[ -z "$AWS_ACCESS_KEY_ID" || -z "$AWS_SECRET_ACCESS_KEY" ]]; then
            echo "ΓÜá∩╕Å  AWS_ACCESS_KEY_ID or AWS_SECRET_ACCESS_KEY secret is not set."
            echo "   Add them in: Settings ΓåÆ Secrets and variables ΓåÆ Actions"
            echo "   Skipping Terraform Apply."
            echo "skip=true" >> $GITHUB_OUTPUT
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

      - name: Setup Terraform
        if: steps.check-creds.outputs.skip != 'true'
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "~1.7"
          terraform_wrapper: false   # raw output for apply (no wrapper wrapping)

      # ΓöÇΓöÇ Initialise ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
      # Backend config is fully defined in backend.tf ΓÇö no -backend-config
      # overrides needed. Passing empty secrets as -backend-config args would
      # cause "bucket must not be empty" errors.
      # If the S3 backend bucket does not exist yet, init will fail ΓÇö we catch
      # that and skip the rest of the job gracefully.
      - name: Terraform Init
        id: init
        if: steps.check-creds.outputs.skip != 'true'
        run: terraform init -reconfigure
        continue-on-error: true

      - name: Skip if backend unavailable
        if: steps.check-creds.outputs.skip != 'true' && steps.init.outcome != 'success'
        run: |
          echo "ΓÜá∩╕Å  terraform init failed ΓÇö S3 backend may not exist yet." >> $GITHUB_STEP_SUMMARY
          echo "   Create the state bucket and DynamoDB table (see backend.tf)" >> $GITHUB_STEP_SUMMARY
          echo "   then re-run this workflow." >> $GITHUB_STEP_SUMMARY
          echo "backend_ready=false" >> $GITHUB_ENV

      # ΓöÇΓöÇ Validate before applying ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
      - name: Terraform Validate
        if: steps.check-creds.outputs.skip != 'true' && steps.init.outcome == 'success'
        run: terraform validate -no-color

      # ΓöÇΓöÇ Plan (fresh plan ΓÇö never apply a stale one) ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
      - name: Terraform Plan
        id: plan
        if: steps.check-creds.outputs.skip != 'true' && steps.init.outcome == 'success'
        run: |
          set -o pipefail
          terraform plan \
            -no-color \
            -input=false \
            -out=tfplan.bin \
            -var="environment=production" \
            2>&1 | tee plan.txt

      # ΓöÇΓöÇ Show what will change ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
      - name: Show plan summary
        if: steps.check-creds.outputs.skip != 'true' && steps.init.outcome == 'success'
        run: |
          echo "### Terraform Plan Summary" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          (grep -E '(Plan:|No changes|must be|will be)' plan.txt || true) >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

      # ΓöÇΓöÇ Apply ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
      - name: Terraform Apply
        id: apply
        if: steps.check-creds.outputs.skip != 'true' && steps.init.outcome == 'success'
        run: |
          set -o pipefail
          terraform apply \
            -no-color \
            -input=false \
            -auto-approve \
            tfplan.bin \
            2>&1 | tee apply.txt

      # ΓöÇΓöÇ Capture outputs ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
      - name: Terraform Output
        id: output
        if: steps.check-creds.outputs.skip != 'true' && steps.init.outcome == 'success' && steps.apply.outcome == 'success'
        run: terraform output -no-color -json | tee outputs.json
        continue-on-error: true

      # ΓöÇΓöÇ Upload outputs as artifact ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
      - name: Upload Terraform outputs
        uses: actions/upload-artifact@v4
        if: steps.check-creds.outputs.skip != 'true' && steps.output.outcome == 'success'
        with:
          name: terraform-outputs-${{ github.sha }}
          path: terraform/outputs.json
          retention-days: 30

      # ΓöÇΓöÇ Diagnose on failure ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
      - name: Show state on failure
        if: failure() && steps.check-creds.outputs.skip != 'true'
        run: |
          echo "Apply failed ΓÇö current state:"
          terraform state list || true
          echo ""
          echo "Last 100 lines of apply output:"
          tail -100 apply.txt || true

      # ΓöÇΓöÇ Write summary ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
      - name: Write apply summary
        if: always()
        run: |
          echo "## Terraform Apply Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [[ "${{ steps.check-creds.outputs.skip }}" == "true" ]]; then
            echo "ΓÜá∩╕Å **Skipped** ΓÇö AWS credentials not configured." >> $GITHUB_STEP_SUMMARY
            exit 0
          fi
          echo "| Step | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|---|---|" >> $GITHUB_STEP_SUMMARY
          PLAN_STATUS="${{ steps.plan.outcome }}"
          APPLY_STATUS="${{ steps.apply.outcome }}"
          [[ "$PLAN_STATUS"  == "success" ]] && PLAN_ICON="Γ£à"  || PLAN_ICON="Γ¥î ($PLAN_STATUS)"
          [[ "$APPLY_STATUS" == "success" ]] && APPLY_ICON="Γ£à" || APPLY_ICON="Γ¥î ($APPLY_STATUS)"
          echo "| Plan  | $PLAN_ICON  |" >> $GITHUB_STEP_SUMMARY
          echo "| Apply | $APPLY_ICON |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Actor:** @${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          if [ -f outputs.json ]; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### Key Outputs" >> $GITHUB_STEP_SUMMARY
            echo '```json' >> $GITHUB_STEP_SUMMARY
            cat outputs.json >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          fi
